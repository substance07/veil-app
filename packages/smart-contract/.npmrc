# Cấu hình để package chạy độc lập, không phụ thuộc vào workspace
# Không tự động link các workspace packages
legacy-peer-deps=true
prefer-offline=false
# Đảm bảo npm chỉ đọc package.json trong thư mục hiện tại
package-lock=true

